class Project {
  final String title;
  final String description;
  final String technologies;

  // This is the constructor. It tells Flutter exactly what information 
  // is required whenever you try to create a new Project.
  Project({
    required this.title,
    required this.description,
    required this.technologies,
  });
}